# Device settings
ASR_DEVICE=rocm
ASR_MODELS_ROOT=/app/models

# Server settings
PORT=9854
HOST=0.0.0.0
LOG_LEVEL=info

# Model settings
DEFAULT_MODEL=turbo
DEFAULT_LANGUAGE=ru

# Processing settings
AUDIO_SPEEDUP=1.25
MAX_UPLOAD_SIZE_MB=50  # 50MB in bytes

# Cache settings
CACHE_DIR=/app/cache
ENABLE_CACHE=true

# Security settings
CORS_ORIGINS=*
MAX_REQUESTS_PER_MINUTE=60

